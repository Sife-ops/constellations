query User {
  user {
    id
    email
    username
    bookmarks {
      id
      url
      description
      categories {
        id
        name
      }
    }
    categories {
      id
      name
      bookmarks {
        id
        url
        description
        categories {
          id
          name
        }
      }
    }
  }
}
