mutation Register($email: String!, $username: String!, $password: String!, $captcha: String) {
  register(email: $email, username: $username, password: $password, captcha: $captcha) {
    id
    email
    username
  }
}
